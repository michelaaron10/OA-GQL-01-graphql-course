fragment UserInfo on User {
  id
  name
}

query Basic {
  events {
    id
    title
    period { start end }
    organizer { ...UserInfo }
    participants { ...UserInfo }
  }
  users { ...UserInfo }
}
